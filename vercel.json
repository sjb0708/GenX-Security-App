{
  "version": 2,
  "builds": [
    {
      "src": "server.js",
      "use": "@vercel/node",
      "config": { "includeFiles": ["public/**", "demo-data.json", "demo-users.json"] }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "server.js" }]
}
